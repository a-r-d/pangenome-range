# 1000G bounded-pilot status

The existing inputs were confirmed: a 17,771,541,912-byte authenticated GBZ, a
17,051,191,557-byte standalone GBWT, and an 8,975,880,203-byte unchanged v1 graph
archive. No 1000G identity pilot was launched after the smaller HPRC r-index build
exhausted the host.

Accordingly, this directory contains no fabricated membership, codec, overhead, or
query values. The blank CSVs are explicit missing-evidence markers. A whole-source
conversion remains unauthorized; even a bounded query needs an independently built
and measured locate index before it can start safely.
