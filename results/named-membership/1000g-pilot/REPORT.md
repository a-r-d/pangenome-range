# 1000 Genomes exclusion record

No 1000G job was launched. The user explicitly waived this pilot after the earlier required index-building path caused a host out-of-memory failure and declared `EXPERIMENT_COMPLETE_WITH_1000G_EXCLUSION` acceptable as `GO_EXTENSION`.

This directory exists so the required result set records absence rather than silently omitting or inventing evidence. It contains no archive, timing, memory, membership, or overhead claim.
