# 1000 Genomes exclusion record

No 1000G job was launched. The user made this a permanent operational resource-safety exclusion after the earlier HPRC `vg` r-index prerequisite exhausted host memory, and declared `EXPERIMENT_COMPLETE_WITH_1000G_EXCLUSION` acceptable as `GO_EXTENSION`.

The current direct encoder uses embedded DA samples and does not build that r-index. It was not attempted on 1000G, so this record does not claim that the direct implementation failed or passed at 5,008-haplotype scale.

This directory exists so the required result set records absence rather than silently omitting or inventing evidence. It contains no archive, timing, memory, membership, or overhead claim.
