{
  "name": "@pangenome-range/docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fixtures": "node ../scripts/prepare-docs-fixtures.mjs",
    "dev": "pnpm fixtures && vitepress dev .",
    "build": "pnpm fixtures && vitepress build .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/docs.test.mjs",
    "test:pages": "node test/pages-smoke.mjs"
  },
  "dependencies": {
    "pangenome-range": "workspace:*",
    "vue": "^3.5.41"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "typescript": "^6.0.3",
    "vitepress": "^1.6.4"
  }
}
