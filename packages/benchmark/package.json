{
  "name": "@pangenome-range/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter pangenome-range build && node scripts/node-range.mjs",
    "test:browser": "pnpm --filter pangenome-range build && node scripts/browser-range.mjs"
  },
  "dependencies": {
    "pangenome-range": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
