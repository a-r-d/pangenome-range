# Benchmarks

Benchmark harnesses will be added with the first candidate layout. They must use
release builds, opt in to large data, retain raw evidence under `results/`, and
pass the canonical correctness gate described in `docs/BENCHMARKS.md` before a
timing is reported.
